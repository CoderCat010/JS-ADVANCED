function submitBtn(){
    // input box
    const input = document.getElementById('inputBox');
    const inputText = input.value;
    
    // error handleling
    try{
        console.log(bBaria);
    }
    catch(err){
        console.log('ERROR: ', err);
    }
}